import "std/io";

fun main() {
    println(fib(40));
}

fun fib(n: int64) -> int64 {
    if n <= 1 {
        return n;
    } else {
        return fib(n - 1) + fib(n - 2);
    }
}

fun test() {
    let s = "Hello, World!";
    s = "Goodbye, World!";
    println(s);
}
