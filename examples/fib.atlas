import "std/io";

fun main() {
    println(fib_wrapper(15));
}

fun fib(n: int64) -> int64 {
    if n <= 1 {
        return n;
    } else {
        return fib(n - 1) + fib(n - 2);
    }
}

fun fib_wrapper(n: int64) -> int64 {
    return fib(n);
}