// Only println_int64 will work with the new cranelift backend for now.
import "std/io";

fun main() {
    println_int64(fib(40));
}

fun fib(n: int64) -> int64 {
    if n <= 1 {
        return n;
    } else {
        return fib(n - 1) + fib(n - 2);
    }
}

