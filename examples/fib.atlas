import "std/io";

fun main() {
    //Standard benchmark across all Atlas77 versions
    println(fib(40));
}

fun fib(n: int64) -> int64 {
    if n <= 1 {
        return n;
    } else {
        return fib(n - 1) + fib(n - 2);
    }
}