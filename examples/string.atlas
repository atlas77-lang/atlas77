import "std/io";

struct String {
public:
    s: string;
    String(input: string) {
        this.s = input;
    }
}

fun main() {
    let str = new String("Hello, Atlas!");
    print("Original string: ");
    println(str.s);
}
