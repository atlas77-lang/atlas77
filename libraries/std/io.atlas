//TODO: all those functions should only takes reference to avoid copying datas.
public extern println<T>(s: T);
public extern print<T>(s: T);
public extern input() -> string;
public extern panic<T>(s: T);
